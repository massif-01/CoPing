# CoPing 产品需求文档

版本：0.4
状态：v1 已实现，等待真实 Bark Device Key 验收
平台：macOS 14 及以上

## 1. 产品概述

CoPing 是一个 macOS 菜单栏工具。

当本地 Codex 桌面端完成任务，或发起权限请求、普通问题时，CoPing 将简短通知推送到用户的 iPhone。第一版使用 Bark 作为推送渠道，后续增加 ntfy。

CoPing 不负责远程控制 Codex，也不开发 iPhone 客户端。

## 2. 要解决的问题

Codex 执行任务时，用户经常离开电脑或切换到其他设备。用户必须反复回到 Mac 查看任务状态，才能知道：

- 任务是否已经完成；
- 任务是否执行失败；
- Codex 是否正在等待权限批准；
- Codex 是否正在等待用户回答普通问题。

CoPing 的价值是减少无效查看，让用户只在真正需要时回到 Mac。

## 3. 产品目标

第一版只实现一个闭环：

1. 用户通过图形界面配置 Bark；
2. CoPing 自动完成与本地 Codex 的连接；
3. Codex 产生受支持的关键状态时，CoPing 向 iPhone 发送通知；
4. 除首次连接时在 CoPing 引导下完成一次 Codex Hook 审核外，用户不需要使用终端或编写配置文件。

## 4. 非目标

第一版不包含：

- 在 iPhone 上回复 Codex；
- 在 iPhone 上批准或拒绝操作；
- 查看或同步完整 Codex 对话；
- 远程启动、暂停或终止任务；
- CoPing 自有云服务或用户账号；
- CoPing iOS App；
- 执行失败通知；
- 团队协作、消息共享或后台管理；
- ntfy 支持。

## 5. 目标用户

主要用户是使用 macOS Codex 桌面端的个人用户。

第一版最低支持 macOS 14，不将 Codex CLI 列入支持范围。

用户会安装普通 macOS App，但不应被要求：

- 理解 Codex hooks；
- 手动修改配置文件；
- 部署 CoPing 自有服务器。

首次连接允许 CoPing 自动打开一次终端。用户只需按界面说明进入 `/hooks` 并信任 CoPing，不需要输入安装命令。

用户可以直接使用 Bark 公共服务，也可以填写自己的 Bark 服务地址。

## 6. 第一版功能

### 6.1 菜单栏入口

CoPing 常驻 macOS 菜单栏。

菜单栏展示以下状态：

- 已启用；
- 已暂停；
- 配置异常；
- 最近一次推送失败。

点击菜单栏图标后，可进入设置、暂停通知、发送测试通知或退出 CoPing。

CoPing 默认随 macOS 登录自动启动。用户可以在设置中关闭开机启动。

### 6.2 Bark 配置

设置界面包含：

- Bark 服务地址；
- Bark Device Key；
- 通知标题前缀，默认使用 `CoPing`；
- 启用或暂停通知；
- “发送测试通知”按钮。

配置内容在本机保存。Device Key 视为敏感信息，不写入日志或通知正文。

### 6.3 Codex 连接

CoPing 提供“连接 Codex”按钮，自动完成所需配置。

界面需要明确显示：

- 是否已检测到 Codex；
- 是否已经连接；
- 配置是否被外部修改；
- 修复连接入口。

CoPing 修改 Codex 配置前必须保留原配置，并且只修改与 CoPing 直接相关的通知或 hook 配置。

用户不需要了解底层 helper、hook 或配置文件。

### 6.4 通知事件

第一版推送以下事件：

| 事件 | 含义 | 默认推送 |
|---|---|---|
| 任务完成 | Codex 已结束当前任务并给出结果 | 是 |
| 权限请求 | Codex 发起权限请求，5 秒后任务仍未结束 | 是 |
| 普通问题 | Codex 发起普通问题，5 秒后任务仍未结束 | 是 |

不推送运行过程、工具调用、阶段性进度或普通状态变化，避免通知噪音。

执行失败在 v1 中明确标记为暂不支持，不从内部数据库或启发式状态猜测。

### 6.5 通知内容

通知应帮助用户判断是否需要回到 Mac，但不携带过多工作内容。

建议格式：

```text
标题：CoPing · 需要介入
内容：ChatRaw_server — Codex 正在等待权限批准
```

通知最多包含：

- 状态；
- 项目或工作目录名称；
- 一句通用原因。

默认不包含任务摘要、源码、命令输出、文件路径、完整提示词和完整回复。

第一版不提供任务摘要开关。

### 6.6 运行记录

CoPing 只保留有限的本地记录，用于确认系统是否工作：

- 发生时间；
- 通知类型；
- 项目名称；
- 发送成功或失败；
- 不包含 Device Key 和完整 Codex 消息。

界面展示最近若干条记录，并提供清空入口。

## 7. 核心用户流程

### 首次使用

1. 用户安装并打开 CoPing；
2. CoPing 检测本地 Codex；
3. 用户填写 Bark 服务地址和 Device Key；
4. 用户点击“发送测试通知”；
5. iPhone 收到测试通知；
6. 用户点击“连接 Codex”；
7. CoPing 启用登录时自动启动；
8. CoPing 显示“已连接，等待 Codex 事件”。

### 日常使用

1. 用户在 Mac 上交给 Codex 一个任务；
2. 用户离开电脑；
3. Codex 完成或发起可能需要介入的请求；
4. CoPing 通过 Bark 推送通知；
5. 用户根据通知决定是否回到 Mac。

## 8. 隐私与安全

- CoPing 第一版不建设云端服务；
- Codex 事件从本机直接发送至用户配置的 Bark 服务；
- Device Key 只保存在本机；
- 日志不得记录 Device Key、完整对话或完整命令；
- 通知摘要需要限制长度；
- 用户可以关闭摘要，只发送状态和项目名；
- 自动配置 Codex 时，只修改必要字段，并支持恢复。

## 9. 产品形态与技术边界

- 原生 macOS 菜单栏 App；
- 使用 Swift 和 SwiftUI；
- 最低支持 macOS 14；
- 不使用 Electron；
- 不要求 Python、Node.js 等额外运行时；
- 通过本地 Codex 官方事件接口或 hooks 获取状态；
- 不通过轮询或解析不稳定的内部数据库猜测状态；
- 第一版只支持 Codex 桌面端；
- CoPing 随 macOS 登录自动启动，用户可以在设置中关闭；
- 第一版仅包含 Bark Provider；
- 推送通道采用可替换设计，以便第二阶段增加 ntfy。

CoPing 预计通过签名并公证的安装包直接分发。是否进入 Mac App Store 不属于第一版目标。

## 10. 第一版验收标准

第一版完成需要同时满足：

1. 全新用户可以通过图形界面和一次受引导的 Hook 终端审核完成配置；
2. Bark 测试通知能够真实到达 iPhone；
3. Codex 任务完成后能够真实触发 iPhone 通知；
4. Codex 请求权限时能够真实触发 iPhone 通知；
5. Codex 提出普通问题时能够真实触发 iPhone 通知；
6. 登录 macOS 后 CoPing 自动启动并显示菜单栏入口；
7. 重启 Mac 后无需重新配置；
8. Bark 暂时不可用时不影响 Codex 正常运行；
9. CoPing 不泄露 Device Key 或完整 Codex 内容；
10. 断开 CoPing 后，仅移除 CoPing 自己的 Hook。

## 11. 后续版本

第二阶段增加 ntfy：

- 支持官方 ntfy.sh；
- 支持自建 ntfy 服务；
- 支持 Topic、Token 和优先级配置；
- Bark 与 ntfy 在界面中作为可选推送通道；
- 保持相同的 Codex 事件识别逻辑。

暂不承诺同时向多个渠道推送。

## 12. 待确认事项

### 12.1 已通过桌面端实测

2026-07-27 使用 Codex 桌面端真实任务验证：

| 事件 | 结果 | 识别方式 |
|---|---|---|
| 任务完成 | 已通过 | `Stop` |
| 权限请求 | 已通过 | `PermissionRequest` |
| 普通问题 | 已通过 | `PreToolUse` 中的 `request_user_input` |
| 执行失败 | v1 不支持 | 当前 hooks 未提供可靠的失败事件 |

探针只记录事件类型、任务标识、项目名等最小元数据，没有记录提示词、回复正文、命令内容或 Device Key。

### 12.2 实现后的剩余验收

1. 使用真实 Bark Device Key 完成一次从 Codex 桌面事件到 iPhone 到达的端到端验证。
2. 在安装完整 Xcode 和 Developer ID 证书后完成正式签名、公证与干净机器验证。
